# ZHELTA-generated full-stack project — environment template.
# See frontend/.env.example and backend/.env.example for per-tier vars.

# Backend
MONGO_URL=mongodb://localhost:27017
DB_NAME=app_db
JWT_SECRET=change-me

# Frontend (built into the bundle by Vite)
VITE_API_BASE_URL=
